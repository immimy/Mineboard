workflow:
  rules:
    # Prevent push pipelines when the commit title contains "draft" or "wip"
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_COMMIT_TITLE =~ /\b(draft|wip)\b/i'
      when: never
    # Preventing pipeline from MR
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    # Allow branch and schedule pipeline
    - if: '$CI_COMMIT_BRANCH'

stages:
  - build
  - test
  - deploy_preview
  - deploy_prod

# ----- BUILD DOCKER IMAGE -------------------------
# Only run on schedule pipeline

build_docker_image:
  stage: .pre
  image: docker:27
  services:
    - docker:27-dind
  rules:
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
  script:
    - docker version
    - echo $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER --password-stdin $CI_REGISTRY
    - docker build -t $CI_REGISTRY_IMAGE/vercel -f ci/Dockerfile .
    - docker image ls
    - docker push $CI_REGISTRY_IMAGE/vercel

# ----- BUILD -------------------------

build_preview:
  stage: build
  image: $CI_REGISTRY_IMAGE/vercel
  rules:
    - if: '$CI_COMMIT_REF_NAME != $CI_DEFAULT_BRANCH'
  environment: preview/$CI_COMMIT_REF_SLUG
  script:
    - node --version
    - npm --version
    - vercel --version
    - vercel pull --yes --environment=preview --token $VERCEL_TOKEN
    - vercel build --token $VERCEL_TOKEN
  artifacts:
    paths:
      - .vercel/output/
      - .vercel/project.json
      - .next/
      - node_modules/

build_prod:
  stage: build
  image: $CI_REGISTRY_IMAGE/vercel
  rules:
    - if: '$CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH'
  environment: production
  script:
    - node --version
    - npm --version
    - vercel --version
    - vercel pull --yes --environment=production --token $VERCEL_TOKEN
    - vercel build --prod --token $VERCEL_TOKEN
  artifacts:
    paths:
      - .vercel/output/
      - .vercel/project.json
      - .next/
      - node_modules/

# ----- TEST -------------------------

unit_tests:
  stage: test
  image: mcr.microsoft.com/playwright:v1.62.1-noble
  script:
    - npm ci
    - npm test
  artifacts:
    when: always
    reports:
      junit: reports/junit*.xml

# ----- DEPLOY -------------------------

deploy_preview:
  stage: deploy_preview
  image: $CI_REGISTRY_IMAGE/vercel
  rules:
    - if: '$CI_COMMIT_REF_NAME != $CI_DEFAULT_BRANCH'
  environment:
    name: preview/$CI_COMMIT_REF_SLUG
    url: $PREVIEW_URL
  script:
    - vercel --version
    - PREVIEW_URL=$(vercel deploy --prebuilt --archive=tgz --regions sin1 --token $VERCEL_TOKEN)
    - echo "PREVIEW_URL=$PREVIEW_URL" > deploy.env
    - cat deploy.env
    # Provide a bypass token to access the protected deployments (Preview environment)
    # Only matches HTML elements containing "Mineboard"
    - curl "$PREVIEW_URL/api/webhook?x-vercel-protection-bypass=$VERCEL_AUTOMATION_BYPASS_SECRET" | grep -oE '<[a-z][a-zA-Z0-9]*[^>]*>[^<]*Mineboard[^<]*</[a-z][a-zA-Z0-9]*>'
  artifacts:
    reports:
      dotenv: deploy.env

deploy_prod:
  stage: deploy_prod
  image: $CI_REGISTRY_IMAGE/vercel
  rules:
    - if: '$CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH'
  environment: production
  before_script:
    # Ensure that all edge functions are deployed
    # and migrations are pushed to Supabase
    - npx supabase login --token $SUPABASE_ACCESS_TOKEN
    - npx supabase link --project-ref $SUPABASE_PROJECT_REF
    - npx supabase functions deploy
    - npx supabase migration list
    - npx supabase db push
  script:
    - vercel --version
    - PRODUCTION_URL=$(vercel deploy --prebuilt --archive=tgz --regions sin1 --prod --token $VERCEL_TOKEN)
    - echo $PRODUCTION_URL
    # Provide a bypass token to access the protected deployments (Production environment)
    # Only matches HTML elements containing "Mineboard"
    - curl "$PRODUCTION_URL/api/webhook?x-vercel-protection-bypass=$VERCEL_AUTOMATION_BYPASS_SECRET" | grep -oE '<[a-z][a-zA-Z0-9]*[^>]*>[^<]*Mineboard[^<]*</[a-z][a-zA-Z0-9]*>'
